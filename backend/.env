# Server
PORT=4000

# Database (Prisma + Postgres)
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/vetcare
PGHOST=localhost
PGPORT=5432
PGDATABASE=vetcare
PGUSER=postgres
PGPASSWORD=postgres

# JWT settings (for login sessions)
JWT_SECRET=super_dev_secret_change_me
JWT_EXPIRES=15m

REFRESH_SECRET=another_secret_dev_only_change_me
REFRESH_EXPIRES=7d

# Make secrets explicit & stable
ACCESS_TOKEN_SECRET=super_access_secret_change_me
REFRESH_TOKEN_SECRET=super_refresh_secret_change_me
